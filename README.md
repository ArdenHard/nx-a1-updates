# NX-A1 stable OTA channel

This directory publishes only a signed manifest. Encrypted application packages and signatures are release assets.
NX-A1 devices do not contain GitHub credentials and consume this channel over HTTPS.
